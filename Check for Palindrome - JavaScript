
function palindrome(str) {
  //Eliminate non alpha-numeric characters
  str=str.replace(/[^a-zA-Z0-9]/g,'').toLowerCase();
  var revstr=str.split("").reverse().join("");
  
  if(str==revstr)
    return true;
  else
    return false;
}



palindrome("1 eye for of 1 eye.");
